# Frontend TODO

Placeholder for the CHAMP-Pipeline viewer (Vite + React + Tailwind).

- Actual app scaffolding comes in **Phase 5**.
- `package.json` exists only to reserve the workspace name `champ-pipeline-viewer`; no dependencies, scripts, or source yet.
